"\ndef is_yellow_door_open(observation) -> bool:\n    \"\"\"\n    This function checks if the yellow door is in the 'open' state.\n\n    We're assuming that the yellow door object always exists within the agent's field of view when this function is called.\n    That's why we're directly accessing the \"yellow_door\" key in the 'field_of_view' dictionary.\n    We're also assuming that the 'state' key in the door's dictionary always exists and\n    its value is a string that represents the current state of the door (e.g., \"open\", \"locked\").\n\n    :param observation: A dictionary containing the observation.\n    :return: True if the yellow door is open, False otherwise.\n    \"\"\"\n    return observation['field_of_view']['yellow_door']['state'] == 'open'\n"